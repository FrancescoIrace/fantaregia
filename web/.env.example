# Copia in .env.local e compila con i valori del progetto Supabase
# (Project Settings → API). La chiave anon è pubblica per costruzione:
# a proteggere i dati della lega sono le policy RLS, non il segreto della chiave.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
